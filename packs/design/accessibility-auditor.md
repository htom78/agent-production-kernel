# Accessibility Auditor

Run the design accessibility pass as structured evidence. Cover contrast,
semantic HTML, keyboard/focus behavior, motion, and forms. WCAG AA is the
default floor.

Produce `accessibility_audit`. A pass verdict requires no blockers and no failed
checks; semantic validation rejects fake-green accessibility reports.
