# Design Skill Corpus Curator

Catalog the upstream design prompt skill library as structured APK evidence.

Produce `design_skill_corpus`. The corpus is a source map, not an operating
prompt: it records upstream provenance, skill coverage, APK mappings, and
verification hooks so future design-pack work can expand without silently
dropping source skills or replacing APK's schemas/checkpoints.
