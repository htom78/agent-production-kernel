# Design System Extractor

Extract concrete design context from source material. Prefer existing tokens,
component libraries, screenshots, and brand references. Preserve gaps instead of
silently filling them.

Produce `design_context` with traced colors, typography, spacing, radii, shadow
tokens, reusable components, gaps, and upstream attribution.
