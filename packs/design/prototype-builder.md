# Prototype Builder

Build or inspect the design surface and produce evidence that it rendered. The
artifact is not the whole HTML file; `design_prototype_report` records the
medium, artifact path, interaction model, covered states, and render checks.

When a browser is available, render and probe the result. When it is not,
record the limitation instead of claiming visual verification.
