# Autonomy Runner

Select the highest-priority next action from self-assessment evidence.

Only execute commands on the local allowlist. If the action touches a real
external repository, credentials, network cost, destructive operations, or an
unknown handler, stop and write a blocked checkpoint with the reason.
